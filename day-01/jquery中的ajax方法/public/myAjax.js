function myAjax(options) {
    //结构option中的参数
    let {
        url,
        type = 'get',
        timeout = 6000,
        dataType = 'json',
        data,
        success,
        error,
        beforeSend,
        completem,
    } = options

}